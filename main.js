const myLibrary = [];

function newBook() {
    let book = prompt("Give me a book");
    
}

function Book(title, author, pages, read) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.read = read;
}
